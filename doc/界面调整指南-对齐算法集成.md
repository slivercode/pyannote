# pyannote-audio 对齐算法调用流程与界面调整指南

## 📊 当前调用流程分析

### 1. 调用链路

```
前端界面 (index.html)
    ↓ 上传SRT + 选择TTS配置
API接口 (/api/tts-dubbing/start)
    ↓ 创建后台任务
TTSDubbingProcessor (tts_dubbing_processor.py)
    ↓ 解析SRT + 调用TTS API
    ↓ 生成配音文件
SpeedRateAdjuster (speed_rate_adjuster.py)  ← 🎯 对齐算法在这里
    ↓ 音频加速 + 拼接
最终音频输出
```

### 2. 对齐算法调用位置

**文件**: `src/scripts/tts_dubbing_processor.py`  
**方法**: `process()` 方法中的第3步

```python
# 第3步：判断是否使用智能双重变速机制
if self.enable_smart_speedup:
    print("\n🚀 启用智能双重变速机制...")
    
    # 创建双重变速调整器
    adjuster = SpeedRateAdjuster(
        subtitles=subtitle_data,
        audio_files=audio_files,
        output_dir=str(self.output_dir),
        enable_audio_speedup=self.enable_audio_speedup,
        enable_video_slowdown=self.enable_video_slowdown,  # 当前未实现
        max_audio_speed_rate=self.max_audio_speed_rate,
        max_video_pts_rate=self.max_video_pts_rate,
        remove_silent_gaps=self.remove_silent_gaps,
        align_subtitle_audio=self.auto_align,
        raw_total_time_ms=raw_total_time_ms
    )
    
    # 执行双重变速处理
    output_path, updated_subtitles = adjuster.process()
```

### 3. 当前支持的功能

✅ **已实现**:
- 音频加速（rubberband/atempo）
- 智能策略选择
- 音频片段合并
- 字幕时间轴对齐

❌ **未实现**:
- 视频慢速处理
- 视频文件上传和处理
- 物理时间轴模型

---

## 🎨 界面调整方案

根据你提供的截图，当前界面包含：
- 多角色配音配置
- TTS引擎选择
- 角色映射配置
- 语速控制（0.5x - 2.0x）
- 自动对齐时间轴选项

### 需要添加的界面元素

#### 方案1: 最小化调整（推荐⭐⭐⭐⭐⭐）

在现有界面上添加一个"高级选项"折叠面板：

```html
<!-- 在"自动对齐时间轴"选项后添加 -->
<div class="mb-4">
  <label class="flex items-center cursor-pointer">
    <input 
      type="checkbox" 
      v-model="ttsDubbing.enableSmartSpeedup"
      class="mr-2"
    >
    <span class="text-sm">
      <i class="fas fa-magic mr-1 text-primary"></i>
      启用智能双重变速机制
    </span>
  </label>
  <p class="text-xs text-gray-500 ml-6 mt-1">
    自动调整音频速度以匹配字幕时长，提升配音质量
  </p>
</div>

<!-- 高级选项（折叠面板）-->
<div v-if="ttsDubbing.enableSmartSpeedup" class="mb-4 bg-dark p-3 rounded border border-gray-700">
  <div class="flex items-center justify-between mb-3 cursor-pointer" @click="showAdvancedOptions = !showAdvancedOptions">
    <span class="text-sm font-medium">
      <i class="fas fa-sliders-h mr-1 text-primary"></i>
      高级选项
    </span>
    <i :class="showAdvancedOptions ? 'fas fa-chevron-up' : 'fas fa-chevron-down'" class="text-gray-500"></i>
  </div>
  
  <div v-show="showAdvancedOptions" class="space-y-3">
    <!-- 音频加速选项 -->
    <div>
      <label class="flex items-center cursor-pointer">
        <input 
          type="checkbox" 
          v-model="ttsDubbing.enableAudioSpeedup"
          class="mr-2"
        >
        <span class="text-sm">启用音频加速</span>
      </label>
    </div>
    
    <!-- 最大音频加速倍率 -->
    <div v-if="ttsDubbing.enableAudioSpeedup">
      <label class="block text-xs text-gray-400 mb-1">
        最大音频加速倍率
      </label>
      <div class="flex items-center gap-2">
        <input 
          type="range" 
          v-model.number="ttsDubbing.maxAudioSpeedRate"
          min="1.0" 
          max="3.0" 
          step="0.1"
          class="flex-1"
        >
        <span class="text-sm text-white w-12">{{ ttsDubbing.maxAudioSpeedRate }}x</span>
      </div>
      <p class="text-xs text-gray-600 mt-1">
        建议: 1.5x-2.0x（过高会影响音质）
      </p>
    </div>
    
    <!-- 视频慢速选项（未来功能）-->
    <div class="opacity-50 cursor-not-allowed">
      <label class="flex items-center">
        <input 
          type="checkbox" 
          disabled
          class="mr-2"
        >
        <span class="text-sm">启用视频慢速（即将推出）</span>
      </label>
      <p class="text-xs text-gray-600 ml-6 mt-1">
        需要上传视频文件才能使用此功能
      </p>
    </div>
    
    <!-- 移除静音间隙 -->
    <div>
      <label class="flex items-center cursor-pointer">
        <input 
          type="checkbox" 
          v-model="ttsDubbing.removeSilentGaps"
          class="mr-2"
        >
        <span class="text-sm">移除字幕间静音间隙</span>
      </label>
      <p class="text-xs text-gray-600 ml-6 mt-1">
        紧凑拼接配音，适合快节奏内容
      </p>
    </div>
  </div>
</div>
```

#### 方案2: 完整功能界面（未来扩展）

添加视频上传和处理功能：

```html
<!-- 在SRT文件上传后添加 -->
<div class="mb-4">
  <label class="block text-sm font-medium mb-2">
    <i class="fas fa-video mr-1"></i>
    视频文件（可选）
  </label>
  <div class="flex gap-2">
    <input
      type="file"
      accept="video/*"
      @change="handleVideoUpload"
      class="flex-1 bg-dark text-white px-4 py-2 rounded border border-gray-600"
    >
    <button 
      v-if="ttsDubbing.videoFile"
      @click="clearVideo"
      class="px-3 py-2 bg-red-600 hover:bg-red-700 rounded"
    >
      <i class="fas fa-times"></i>
    </button>
  </div>
  <p class="text-xs text-gray-500 mt-1">
    上传视频后可启用视频慢速功能，实现更精确的声画同步
  </p>
</div>
```

---

## 💻 前端代码调整

### 1. 数据模型更新

在 `index.html` 的 Vue data 中添加：

```javascript
data() {
  return {
    ttsDubbing: {
      // ... 现有字段
      
      // 新增：双重变速机制相关
      enableSmartSpeedup: false,        // 是否启用智能变速
      enableAudioSpeedup: true,         // 是否启用音频加速
      enableVideoSlowdown: false,       // 是否启用视频慢速
      maxAudioSpeedRate: 2.0,           // 最大音频加速倍率
      maxVideoSpeedRate: 10.0,          // 最大视频慢速倍率
      removeSilentGaps: false,          // 是否移除静音间隙
      videoFile: null,                  // 视频文件（未来功能）
    },
    
    // 新增：高级选项展开状态
    showAdvancedOptions: false,
  }
}
```

### 2. API 调用更新

修改 `startTtsDubbing` 方法：

```javascript
async startTtsDubbing() {
  try {
    const formData = new FormData();
    formData.append('srt_file', this.ttsDubbing.srtFile);
    formData.append('engine', this.ttsDubbing.engine);
    formData.append('role', JSON.stringify(this.ttsDubbing.selectedRole));
    formData.append('text_lang', this.ttsDubbing.textLang);
    formData.append('speed_factor', this.ttsDubbing.speedFactor);
    formData.append('silence_duration', this.ttsDubbing.silenceDuration);
    formData.append('auto_align', this.ttsDubbing.autoAlign);
    
    // 新增：双重变速机制参数
    formData.append('enable_smart_speedup', this.ttsDubbing.enableSmartSpeedup);
    formData.append('enable_audio_speedup', this.ttsDubbing.enableAudioSpeedup);
    formData.append('enable_video_slowdown', this.ttsDubbing.enableVideoSlowdown);
    formData.append('max_audio_speed_rate', this.ttsDubbing.maxAudioSpeedRate);
    formData.append('max_video_pts_rate', this.ttsDubbing.maxVideoSpeedRate);
    formData.append('remove_silent_gaps', this.ttsDubbing.removeSilentGaps);
    
    // 如果有视频文件
    if (this.ttsDubbing.videoFile) {
      formData.append('video_file', this.ttsDubbing.videoFile);
    }
    
    // ... 其余代码
  } catch (error) {
    // ... 错误处理
  }
}
```

---

## 🔧 后端代码调整

### 1. API 接口更新

修改 `main.py` 中的 `/api/tts-dubbing/start` 接口：

```python
@app.post("/api/tts-dubbing/start", summary="启动TTS配音任务")
async def start_tts_dubbing(
    srt_file: UploadFile = File(...),
    engine: str = Form(...),
    role: str = Form(...),
    text_lang: str = Form('zh'),
    speed_factor: float = Form(1.0),
    silence_duration: float = Form(0.5),
    auto_align: bool = Form(True),
    api_url: str = Form(None),
    api_key: str = Form(None),
    
    # 新增：双重变速机制参数
    enable_smart_speedup: bool = Form(False),
    enable_audio_speedup: bool = Form(True),
    enable_video_slowdown: bool = Form(False),
    max_audio_speed_rate: float = Form(2.0),
    max_video_pts_rate: float = Form(10.0),
    remove_silent_gaps: bool = Form(False),
    video_file: UploadFile = File(None),  # 可选的视频文件
):
    """启动TTS配音任务（支持双重变速机制）"""
    try:
        # ... 现有代码
        
        # 如果上传了视频文件，保存到任务目录
        video_path = None
        if video_file:
            video_path = task_dir / "input_video.mp4"
            with open(video_path, 'wb') as f:
                content = await video_file.read()
                f.write(content)
        
        # 创建处理器时传递新参数
        processor = TTSDubbingProcessor(
            srt_path=str(srt_path),
            output_dir=str(task_dir),
            engine=engine,
            role_data=role_data,
            text_lang=text_lang,
            speed_factor=speed_factor,
            silence_duration=silence_duration,
            auto_align=auto_align,
            api_url=api_url,
            api_key=api_key,
            task_id=task_id,
            task_dict=tts_dubbing_tasks,
            
            # 新增参数
            enable_smart_speedup=enable_smart_speedup,
            enable_audio_speedup=enable_audio_speedup,
            enable_video_slowdown=enable_video_slowdown,
            max_audio_speed_rate=max_audio_speed_rate,
            max_video_pts_rate=max_video_pts_rate,
            remove_silent_gaps=remove_silent_gaps,
            video_file=str(video_path) if video_path else None,
        )
        
        # ... 其余代码
    except Exception as e:
        # ... 错误处理
```

### 2. TTSDubbingProcessor 更新

`tts_dubbing_processor.py` 已经支持这些参数，只需确保传递给 `SpeedRateAdjuster`：

```python
# 在 process() 方法中
if self.enable_smart_speedup:
    adjuster = SpeedRateAdjuster(
        subtitles=subtitle_data,
        audio_files=audio_files,
        output_dir=str(self.output_dir),
        video_file=self.video_file,  # 新增：传递视频文件
        enable_audio_speedup=self.enable_audio_speedup,
        enable_video_slowdown=self.enable_video_slowdown,
        max_audio_speed_rate=self.max_audio_speed_rate,
        max_video_pts_rate=self.max_video_pts_rate,
        remove_silent_gaps=self.remove_silent_gaps,
        align_subtitle_audio=self.auto_align,
        raw_total_time_ms=raw_total_time_ms
    )
    
    output_path, updated_subtitles = adjuster.process()
```

---

## 📋 完整的界面调整清单

### 阶段1: 基础集成（立即实施）

- [ ] 添加"启用智能双重变速机制"复选框
- [ ] 添加"高级选项"折叠面板
- [ ] 添加"最大音频加速倍率"滑块（1.0x - 3.0x）
- [ ] 添加"移除静音间隙"选项
- [ ] 更新前端数据模型
- [ ] 更新 API 调用参数
- [ ] 更新后端接口参数
- [ ] 测试基础功能

### 阶段2: 视频支持（未来扩展）

- [ ] 添加视频文件上传组件
- [ ] 添加"启用视频慢速"选项
- [ ] 添加"最大视频慢速倍率"滑块
- [ ] 实现视频文件处理逻辑
- [ ] 集成完整的 pyvideotrans 对齐算法
- [ ] 测试视频慢速功能

---

## 🎯 推荐实施步骤

### 第1步: 最小化界面调整（1小时）

只添加一个复选框和一个滑块：

```html
<!-- 在"自动对齐时间轴"后添加 -->
<div class="mb-4">
  <label class="flex items-center cursor-pointer">
    <input 
      type="checkbox" 
      v-model="ttsDubbing.enableSmartSpeedup"
      class="mr-2"
    >
    <span class="text-sm">
      <i class="fas fa-magic mr-1 text-primary"></i>
      启用智能音频加速（推荐）
    </span>
  </label>
</div>

<div v-if="ttsDubbing.enableSmartSpeedup" class="mb-4">
  <label class="block text-xs text-gray-400 mb-1">
    最大加速倍率: {{ ttsDubbing.maxAudioSpeedRate }}x
  </label>
  <input 
    type="range" 
    v-model.number="ttsDubbing.maxAudioSpeedRate"
    min="1.0" 
    max="2.0" 
    step="0.1"
    class="w-full"
  >
</div>
```

### 第2步: 后端参数传递（30分钟）

修改 API 调用和后端接口，传递新参数。

### 第3步: 测试验证（30分钟）

- 上传测试 SRT 文件
- 启用智能加速
- 调整加速倍率
- 验证输出音频质量

### 第4步: 完整界面（可选，2小时）

添加完整的高级选项面板和所有配置项。

---

## 📊 界面效果预览

### 简化版（推荐）

```
┌─────────────────────────────────────┐
│ 配音配置                             │
├─────────────────────────────────────┤
│ □ 带说话人标识的SRT文件              │
│ [选择文件] 字幕说话人分配结果.srt    │
│                                     │
│ TTS引擎                             │
│ [GPT-SoVITS ▼]                      │
│                                     │
│ 合成语言                             │
│ [中文 ▼]                            │
│                                     │
│ 语速                                 │
│ [━━━●━━━━━━] 1.0x                   │
│                                     │
│ ☑ 自动对齐时间轴                     │
│ ☑ 启用智能音频加速（推荐）            │
│                                     │
│ 最大加速倍率: 2.0x                   │
│ [━━━━━●━━━━] 1.0x - 2.0x            │
│                                     │
│ [开始配音]                           │
└─────────────────────────────────────┘
```

### 完整版（未来）

```
┌─────────────────────────────────────┐
│ 配音配置                             │
├─────────────────────────────────────┤
│ SRT文件                             │
│ [选择文件] 字幕.srt                  │
│                                     │
│ 视频文件（可选）                     │
│ [选择文件] 视频.mp4                  │
│                                     │
│ ☑ 启用智能双重变速机制                │
│                                     │
│ ▼ 高级选项                           │
│   ☑ 启用音频加速                     │
│   最大加速倍率: [━━●━━] 2.0x        │
│                                     │
│   ☑ 启用视频慢速                     │
│   最大慢速倍率: [━●━━━] 2.0x        │
│                                     │
│   ☐ 移除静音间隙                     │
│                                     │
│ [开始配音]                           │
└─────────────────────────────────────┘
```

---

## 🎉 总结

### 当前状态
- ✅ 对齐算法已实现（仅音频加速）
- ✅ 调用流程已打通
- ❌ 界面缺少配置选项

### 需要调整
1. **前端**: 添加复选框和滑块（最小化：2个控件）
2. **后端**: 传递参数（已支持，只需添加接口参数）
3. **测试**: 验证功能正常工作

### 推荐方案
**立即实施**: 方案1（最小化调整）
- 只需添加 2 个界面元素
- 1-2 小时完成
- 立即可用

**未来扩展**: 方案2（完整功能）
- 添加视频上传和处理
- 需要 1-2 天开发
- 功能更完整

---

**创建日期**: 2024-12-15  
**版本**: v1.0
