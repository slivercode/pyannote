# 动态时间轴调整 - 工作原理图

## 📊 整体流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                     用户上传SRT + 配置TTS                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    解析SRT，提取字幕信息                         │
│  [{start: 0ms, end: 2000ms, text: "第一句"},                    │
│   {start: 2500ms, end: 5000ms, text: "第二句"}, ...]           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    逐条调用TTS生成配音                           │
│  audio_001.wav (2500ms)                                         │
│  audio_002.wav (3200ms)                                         │
│  audio_003.wav (1800ms)                                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────┴────────┐
                    │ 是否启用智能加速？│
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
               否│                        │是
                │                         │
                ▼                         ▼
    ┌───────────────────┐    ┌───────────────────────┐
    │  传统拼接方式      │    │ 是否保持总时长不变？   │
    │  直接按原时间轴    │    └───────┬───────────────┘
    │  拼接音频         │            │
    └─────────┬─────────┘    ┌───────┴───────┐
              │              │               │
              │             否│              │是
              │              │               │
              │              ▼               ▼
              │  ┌───────────────────┐  ┌──────────────────────┐
              │  │ SpeedRateAdjuster │  │ TimelineAdjuster     │
              │  │ 音频加速          │  │ 动态调整时间轴        │
              │  │ 不保证总时长      │  │ 保证总时长不变        │
              │  └─────────┬─────────┘  └──────────┬───────────┘
              │            │                       │
              └────────────┴───────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    输出最终音频和字幕                            │
│  dubbing_result.wav                                             │
│  updated_subtitles.srt                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 TimelineAdjuster 详细流程

```
┌─────────────────────────────────────────────────────────────────┐
│                  TimelineAdjuster.adjust_timeline()             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段1: 获取每段配音的实际时长                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 字幕1: 原2000ms → 配音2500ms (超出500ms)                │   │
│  │ 字幕2: 原2500ms → 配音3200ms (超出700ms)                │   │
│  │ 字幕3: 原2000ms → 配音1800ms (短200ms)                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段2: 计算总时长差异                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 原始总时长: 8000ms                                       │   │
│  │ 配音总时长: 7500ms + 3200ms + 1800ms = 9000ms          │   │
│  │ 时长差异: +1000ms (超出)                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────┴────────┐
                    │  时长差异判断    │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ 差异 < 10ms   │  │ 差异 > 0         │  │ 差异 < 0         │
│ 简单调整      │  │ 压缩时间轴        │  │ 扩展时间轴        │
└───────┬───────┘  └────────┬─────────┘  └────────┬─────────┘
        │                   │                     │
        │                   ▼                     ▼
        │      ┌─────────────────────┐  ┌─────────────────────┐
        │      │ 计算静音间隙总时长   │  │ 计算静音间隙总时长   │
        │      │ 按比例压缩每个间隙   │  │ 按比例扩展每个间隙   │
        │      │ 如间隙不足则紧密排列 │  │ 确保总时长等于原始   │
        │      └──────────┬──────────┘  └──────────┬──────────┘
        │                 │                        │
        └─────────────────┴────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段3: 生成更新后的字幕列表                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 字幕1: 0ms - 2500ms (配音2500ms)                        │   │
│  │ 间隙1: 2500ms - 2800ms (压缩后300ms)                    │   │
│  │ 字幕2: 2800ms - 6000ms (配音3200ms)                     │   │
│  │ 间隙2: 6000ms - 6200ms (压缩后200ms)                    │   │
│  │ 字幕3: 6200ms - 8000ms (配音1800ms)                     │   │
│  │ 最终总时长: 8000ms ✅ (与原始一致)                       │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📐 压缩时间轴示例

### 场景：配音超出原始时长

```
原始SRT时间轴 (总时长: 10秒)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 字幕1: 2s │ 间隙: 0.5s │ 字幕2: 2.5s │ 间隙: 1s │ 字幕3: 2s │ 间隙: 2s
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0s          2s          2.5s         5s         6s        8s        10s

实际配音时长
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 配音1: 2.5s │            │ 配音2: 3s │            │ 配音3: 2.2s │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0s            2.5s         5.5s        8.5s         10.7s
                          ↑ 超出0.7秒

调整后的时间轴 (总时长: 10秒)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 配音1: 2.5s │ 0.3s │ 配音2: 3s │ 0.6s │ 配音3: 2.2s │ 1.4s
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0s            2.5s  2.8s      5.8s  6.4s        8.6s      10s
              ↑ 压缩间隙，总时长保持10秒 ✅
```

---

## 📐 扩展时间轴示例

### 场景：配音短于原始时长

```
原始SRT时间轴 (总时长: 10秒)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 字幕1: 2s │ 间隙: 0.5s │ 字幕2: 2.5s │ 间隙: 1s │ 字幕3: 2s │ 间隙: 2s
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0s          2s          2.5s         5s         6s        8s        10s

实际配音时长
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 配音1: 1.8s │        │ 配音2: 2.2s │        │ 配音3: 1.9s │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0s            1.8s     4s            6.2s       8.1s
                       ↑ 短1.9秒

调整后的时间轴 (总时长: 10秒)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 配音1: 1.8s │ 0.7s │ 配音2: 2.2s │ 1.4s │ 配音3: 1.9s │ 2.0s
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0s            1.8s  2.5s      4.7s  6.1s        8s        10s
              ↑ 扩展间隙，总时长保持10秒 ✅
```

---

## 🎯 关键算法

### 压缩间隙算法

```python
# 1. 计算原始间隙总时长
total_gap = sum(gaps)  # 例如: 500ms + 1000ms + 2000ms = 3500ms

# 2. 计算超出时长
excess_time = 1000ms  # 配音超出1秒

# 3. 计算压缩比例
if total_gap >= excess_time:
    compression_ratio = (total_gap - excess_time) / total_gap
    # 例如: (3500 - 1000) / 3500 = 0.714
    
    # 4. 按比例压缩每个间隙
    compressed_gaps = [gap * compression_ratio for gap in gaps]
    # 例如: [500*0.714, 1000*0.714, 2000*0.714] = [357ms, 714ms, 1428ms]
    # 总间隙: 357 + 714 + 1428 = 2499ms ≈ 2500ms (减少1000ms) ✅
else:
    # 间隙不足，全部移除
    compressed_gaps = [0, 0, 0]
```

### 扩展间隙算法

```python
# 1. 计算原始间隙总时长
total_gap = sum(gaps)  # 例如: 500ms + 1000ms + 2000ms = 3500ms

# 2. 计算短缺时长
shortage_time = 1000ms  # 配音短1秒

# 3. 计算扩展比例
expansion_ratio = (total_gap + shortage_time) / total_gap
# 例如: (3500 + 1000) / 3500 = 1.286

# 4. 按比例扩展每个间隙
expanded_gaps = [gap * expansion_ratio for gap in gaps]
# 例如: [500*1.286, 1000*1.286, 2000*1.286] = [643ms, 1286ms, 2572ms]
# 总间隙: 643 + 1286 + 2572 = 4501ms ≈ 4500ms (增加1000ms) ✅
```

---

## 🔄 与传统方式对比

### 传统方式 (SpeedRateAdjuster)

```
┌──────────────────────────────────────────────────────────┐
│ 1. 生成配音                                              │
│ 2. 音频加速（如果超出字幕时长）                          │
│ 3. 按原始时间轴拼接                                      │
│ 4. 输出                                                  │
│                                                          │
│ 问题：                                                   │
│ - 总时长可能与原始SRT不一致                              │
│ - 字幕时间轴未调整                                       │
│ - 音画可能不同步                                         │
└──────────────────────────────────────────────────────────┘
```

### 新方式 (TimelineAdjuster)

```
┌──────────────────────────────────────────────────────────┐
│ 1. 生成配音                                              │
│ 2. 计算实际时长                                          │
│ 3. 动态调整字幕时间轴                                    │
│ 4. 智能压缩/扩展静音间隙                                 │
│ 5. 按新时间轴拼接                                        │
│ 6. 输出                                                  │
│                                                          │
│ 优势：                                                   │
│ ✅ 总时长与原始SRT完全一致                               │
│ ✅ 字幕时间轴自动调整                                    │
│ ✅ 音画完美同步                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 📊 数据流图

```
┌─────────────┐
│   SRT文件   │
│ (原始时长)  │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  字幕数据                                │
│  [{start_ms, end_ms, text}, ...]        │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  TTS生成配音                             │
│  [audio_001.wav, audio_002.wav, ...]    │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  TimelineAdjuster                       │
│  - 计算实际时长                          │
│  - 计算时长差异                          │
│  - 调整时间轴                            │
└──────┬──────────────────────────────────┘
       │
       ├─────────────────┬─────────────────┐
       ▼                 ▼                 ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ 更新后的    │  │ 最终音频    │  │ 日志信息    │
│ 字幕列表    │  │ (拼接完成)  │  │ (调整详情)  │
└─────────────┘  └─────────────┘  └─────────────┘
       │                 │                 │
       ▼                 ▼                 ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ updated_    │  │ dubbing_    │  │ 控制台输出  │
│ subtitles   │  │ result.wav  │  │             │
│ .srt        │  │             │  │             │
└─────────────┘  └─────────────┘  └─────────────┘
```

---

**说明**：本文档提供了动态时间轴调整功能的可视化说明，帮助理解其工作原理和实现细节。
